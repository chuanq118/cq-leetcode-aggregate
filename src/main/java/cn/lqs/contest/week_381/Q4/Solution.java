package cn.lqs.contest.week_381.Q4;

class Solution {

    // private HashMap<String, Integer> cache;
    public long[] countOfPairs(int n, int x, int y) {
        // long[] ans = new long[n];
        // boolean ok = Math.abs(x - y) > 1;
        // if (!ok) {
        //     for (int i = 1; i <= ans.length; i++) {
        //         ans[i] = n - i;
        //     }
        // }else{
        //     int len = y - x;
        //     for (int i = 1; i <= ans.length; i++) {
        //
        //         ans[i] =;
        //     }
        // }

        // long[] res = new long[n + 1];
        // for (int i = 1; i <= n; i++) {
        //     for (int j = i + 1; j <= n; j++) {
        //         int dist = Math.min(Math.min(Math.abs(i - j),
        //                 Math.abs(i - x) + Math.abs(y - j) + 1),
        //                 Math.abs(i - y) + Math.abs(x - j) + 1);
        //         res[dist] += 1;
        //     }
        // }
        // long[] ans = new long[res.length - 1];
        // for (int i = 0; i < ans.length; i++) {
        //     ans[i] = res[i + 1] * 2;
        // }
        // return ans;
        // this.cache = new HashMap<>();
        // if (x != y) {
        //     int x1 = Math.min(x, y);
        //     int y1 = Math.max(x, y);
        //     this.cache.put(x1 + "#" + y1, 1);
        // }
        // long[] ans = new long[n];
        //
        // return dfs();
        return null;
    }

    // private int dfs(int i, int j) {
    //     if (j == i + 1){
    //         return 1;
    //     }
    // }

}